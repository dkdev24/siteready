---
title: "Deploying to Cloudflare"
description: "Static output, no server required"
---

A static build means there's no server to babysit — Cloudflare Pages just serves the files.

That's the whole pitch: build once, deploy the "dist" folder, done.
